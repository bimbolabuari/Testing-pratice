const reverseString = require('./reverseString');

test('adds coding to equals gnidoc', () => {
  expect(strLength('coding')).toBe('gnidoc');
});

test('adds helper to equals repleh', () => {
    expect(strLength('helper')).toBe('repleh');
  });

  test('adds technology to equals ygolonhcet', () => {
    expect(strLength('technology')).toBe('ygolonhcet');
  });